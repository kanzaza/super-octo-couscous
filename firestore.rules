rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Traffic fine insight records
    match /traffic_fines/{recordId} {
      allow read, write: if true;
    }
  }
}
